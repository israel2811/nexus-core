# Sample input

This is a tiny public sample for validating the offload scripts. Do not replace it with private corpus material in this public repository.

## Test text

NEXUS offload exists to keep heavy manifest, dedupe, and splitting work away from the local Windows laptop while preserving Google Drive as the final native document surface.
